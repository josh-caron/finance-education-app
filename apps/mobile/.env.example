# Copy to apps/mobile/.env.local (git-ignored).
# EXPO_PUBLIC_ vars are inlined into the client bundle, so never put secrets here.

# Local API (wrangler dev). Use your machine's LAN IP, not localhost, when
# running on a physical device: http://192.168.x.x:8787
EXPO_PUBLIC_API_URL=http://localhost:8787
